rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // All client-side access is denied. Only the Flask backend
    // (using the Admin SDK / service account) can read or write.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
